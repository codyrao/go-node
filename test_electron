const { app, BrowserWindow } = require('electron');
const game = require('./output/game.node');

console.log('Electron:', process.versions.electron);
console.log('Node:', process.versions.node);
console.log('ABI:', process.versions.modules);
console.log('');
console.log('Testing hello.node:');
const testArray13 = ["3","4", "5", "hello", "world"]

var req = JSON.stringify({gameIDs: testArray13})

console.log('Hello1:', game.ScanGames(req,function(res){
    console.log(res)
}));

app.whenReady().then(() => {
    const win = new BrowserWindow({ width: 800, height: 600 });
    win.loadFile('index.html');
});

app.on('window-all-closed', () => {
    if (process.platform !== 'darwin') {
        app.quit();
    }
});
